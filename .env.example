# Jiurni - Environment Variables Example
# Create a copy of this file named .env in the same directory and add your API keys

# OpenAI API Key (required for story generation)
# Get your API key from https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model to use (optional, defaults to gpt-4-turbo)
# VITE_OPENAI_MODEL=gpt-3.5-turbo

# Story generation temperature (optional, defaults to 0.7)
# Higher values (e.g., 0.8) make output more random, lower values make it more focused and deterministic
# VITE_OPENAI_TEMPERATURE=0.7 